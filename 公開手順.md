# 公開手順（GitHub Pages）

Night Walker / NW watcher を、遠隔地のiPhone・Androidに届けるための手順です。

---

## 全体の流れ

```
1. スプレッドシートの中継役を作る          （約10分）
2. 2つのアプリにURLを書き込む              （約3分）
3. GitHubにアップロードする                （約10分）
4. マニュアルのURLをLINEで送る              （すぐ）
5. 各自がホーム画面に追加して、ペアコードを設定
```

---

## 1. 中継役（Googleスプレッドシート）を作る

1. Googleスプレッドシートを新規作成
2. 拡張機能 → Apps Script
3. 既存のコードをすべて消し、`Code.gs` の中身を貼り付け
4. 「デプロイ」→「新しいデプロイ」→ 種類「ウェブアプリ」
   - 次のユーザーとして実行：**自分**
   - アクセスできるユーザー：**全員**
5. 発行された **ウェブアプリURL** をコピーしておく

> 合言葉（SECRET_TOKEN）の設定は不要です。かわりに「ペアコード」で親子を紐づけます。

### 動作確認

ブラウザで次を開いて `{"ok":true,"empty":true}` と出れば成功です。

```
（コピーしたURL）?pair=NW-TEST01
```

---

## 2. 2つのアプリにURLを書き込む（重要）

**配布前に必ず行ってください。** ここを済ませておくと、利用者は**ペアコードを入れるだけ**で設定が終わります。URLは利用者からは見えません。

### 対象ファイル

| ファイル | 探す文字列 |
|---|---|
| `nightwalker/index.html` | `__WEBAPP_URL__` |
| `watcher/index.html` | `__WEBAPP_URL__` |

### 手順

1. ファイルを右クリック →「このアプリケーションで開く」→ **テキストエディット**
2. `command + F` で `__WEBAPP_URL__` を検索
3. 手順1でコピーしたURLに**そっくり置き換える**（前後の `__` も消す）
4. 保存

各ファイルに1か所ずつ、計2か所です。

> **置き換えを忘れると**、送信先が空のままになり、保護者に何も届きません。
> アップロード後、Night Walker の「今すぐ送信してテスト」で「送信しました」と出るか必ず確認してください。

## 3. GitHubにアップロードする

### 3-1. リポジトリの準備（初回のみ）

1. https://github.com でアカウント作成（無料）
2. 「+」→「New repository」→ 名前を入力
3. **Public** を選択（無料でGitHub Pagesを使うために必要）
4. 「Create repository」

### 3-2. ファイルをアップロード

**必ずリポジトリの一番上（どのフォルダにも入っていない状態）から行ってください。**

1. 「Code」タブを開く（パンくずが `リポジトリ名` だけの状態）
2. 「Add file」→「Upload files」
   - 直接開く場合： `https://github.com/（ユーザー名）/（リポジトリ名）/upload/main`
3. アップロード画面の上部が **リポジトリ名だけ** になっていることを確認
4. 展開したフォルダを**開いて**、中の7項目をまとめてドラッグ&ドロップ
   - `index.html`
   - `version.json`
   - `Code.gs`
   - `manual-walker-a2fkyh3mwo.html`
   - `manual-watcher-a2fkyh3mwo.html`
   - `nightwalker`（フォルダごと）
   - `watcher`（フォルダごと）
5. 「Commit changes」

> **よくある失敗**：フォルダの中に入った状態でアップロードすると `nightwalker/nightwalker/` のように二重になります。必ず一番上から行ってください。

### 3-3. GitHub Pagesを有効にする（初回のみ）

1. 「Settings」→ 左メニュー「Pages」
2. Source: **Deploy from a branch**
3. Branch: **main** ／ フォルダ: **/ (root)** → 「Save」
4. 1〜3分待つ

### 3-4. 確認

```
https://（ユーザー名）.github.io/（リポジトリ名）/
```

月と歩く人のアイコンが2つ表示されれば成功です。アイコンが崩れている場合は、画像がアップロードされていません。

---

## 4. マニュアルのURLを配る

### お子さんへ

```
https://（ユーザー名）.github.io/（リポジトリ名）/manual-walker-a2fkyh3mwo.html
```

### 保護者へ

```
https://（ユーザー名）.github.io/（リポジトリ名）/manual-watcher-a2fkyh3mwo.html
```

マニュアルの中に**アプリを開くボタン**があるので、このURLを送るだけで完結します。

送るときは、次の一文を添えてください。

> **iPhoneの方は必ずSafariで開いてください。**
> LINEの中で開いた場合は、右下の「Safariで開く」を押してください。

---

## 5. 複数の親子で使う場合

**1つのスプレッドシートを、何組でも同時に使えます。**

| 項目 | 内容 |
|---|---|
| ウェブアプリURL | **全員共通**。アプリに埋め込み済みで、利用者からは見えません |
| ペアコード | **親子ごとに違う**（子が作って親に伝える） |

ペアコードが違えば記録が混ざることはありません。

### 知っておいていただきたいこと

- すべての記録が、あなたのスプレッドシートに集まります（名前・位置・通過時刻を含む）
- 他のご家庭に配る場合は、その旨を伝えておくのが望ましいです
- 完全に分けたい場合は、各家庭が自分でスプレッドシートとApps Scriptを用意すれば分離できます

---

## 6. マニュアルURLの扱い

マニュアルのファイル名には、推測されにくい文字列（`a2fkyh3mwo`）を付けてあります。

- トップページからはリンクしていません
- 検索エンジンに載らない設定です
- **URLを知っている人だけが開けます**

万一URLが漏れても、**ペアコードがないと記録は見られません**（約10億通りあり、推測は事実上不可能です）。

---

## 7. 対応する端末

| 端末 | 対応状況 |
|---|---|
| iPhone / iPad | ◎ すべての機能が使えます |
| Android | ○ ほぼすべて使えます（下記の違いあり） |
| パソコン | △ 画面は見られますが、歩行での利用は想定していません |

### Androidでの違い

自動的に切り替わるので、操作は変わりません。

| 機能 | iPhone | Android |
|---|---|---|
| 地図・ルート | Apple Maps | Googleマップ |
| 天気（気温表示のタップ） | 天気アプリ | 天気の検索結果 |
| 位置の確認（保護者側） | 「探す」アプリ | Googleの端末を探すページ |
| 振動での通知 | 非対応 | 対応 |
| ホーム画面への追加 | Safariの共有ボタンから | Chromeが自動で案内 |

---

## 更新したいとき

アプリには**自動更新の仕組み**が入っています。利用者は起動時に案内が出るので、ボタンを押すだけで最新版になります。

### 手順

1. 直したファイルを、同じ場所にアップロードし直す（同名ファイルは上書き）
2. **`version.json` の `version` を上げる**（例：`2.0.0` → `2.0.1`）
3. **各アプリの `APP_VERSION` も同じ値に揃える**

| ファイル | 変更する箇所 |
|---|---|
| `version.json` | `"version": "2.0.1"` |
| `nightwalker/index.html` | `var APP_VERSION = "2.0.1";` |
| `nightwalker/sw.js` | `var APP_VERSION = "2.0.1";` |
| `watcher/index.html` | `var APP_VERSION = "2.0.1";` |
| `watcher/sw.js` | `var APP_VERSION = "2.0.1";` |

`notes` に短い説明を書いておくと、利用者の画面にその文言が表示されます。

```json
{
  "version": "2.0.1",
  "date": "2026-08-05",
  "notes": "ペース計測の精度を改善"
}
```

### 利用者側の動き

- アプリを開いたとき（および他のアプリから戻ったとき）に自動で確認します
- 新しい版があれば、画面上部に案内が出ます
- **「更新」** を押すと、保存された表示データを消して読み込み直します
- **「後で」** を押すと、その版については再度案内しません（さらに新しい版が出れば改めて案内します）
- 入力した記録や設定は消えません
- 圏外では案内は出ず、通常どおり使えます

画面の一番下に、現在のバージョンが小さく表示されます。更新できたかの確認に使えます。

> **`version.json` と `APP_VERSION` が食い違うと、更新案内が出続けます。**
> 必ず同じ値に揃えてください。

---

## アプリの制約（PWAのため）

- **画面を消すと、GPSの記録と保護者への自動送信が止まります**
  → 到着・出発ボタンを押したときは確実に送信されます
  → リアルタイムの位置は「探す」等でご確認ください
- ペースを測るときは、アプリを開いたまま1分ほど歩く必要があります
- LINEは「送り先を選んで送信」する方式です（自動送信はできません）

これらはマニュアルにも記載済みです。
