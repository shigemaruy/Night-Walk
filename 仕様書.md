# Night Walker / NW watcher 仕様書

**2026 寮生Night Walk in 伊勢 支援システム**

| 項目 | 内容 |
|---|---|
| 対象イベント | 名阪国道関ドライブイン → 猿田彦神社（約58.4km／夜通し徒歩） |
| 作成日 | 2026年7月31日 |
| 版 | 2.1 |

---

## 1. システム概要

### 1.1 目的

夜通しの長距離ウォーキングにおいて、

- **歩く本人**が、通過記録・ペース・到着予想を把握し、連絡手段を得る
- **離れた保護者**が、進捗と安否を確認できる

ことを目的とする。

### 1.2 構成

2つのWebアプリ（PWA）と、1つの中継役から成る。

```
  [ Night Walker ]                    [ NW watcher ]
   子・歩く人                           親・見守る人
        |                                    ^
        | POST                               | GET
        | 到着/出発時、3分毎                    | 60秒毎
        v                                    |
  +---------------------------------------------------+
  |  Google Apps Script  +  Googleスプレッドシート        |
  |  （中継と記録の保管）                                 |
  +---------------------------------------------------+
```

補助的に利用する外部サービス：

| サービス | 用途 |
|---|---|
| Open-Meteo API | 天候・気温・湿度の取得 |
| OpenStreetMap | 地図タイル（Leaflet で描画） |
| Apple Maps / Google Maps | 徒歩ルート案内 |
| LINE | メッセージ・写真の送信 |

### 1.3 配置

| 構成物 | 配置先 |
|---|---|
| Night Walker | GitHub Pages `/nightwalker/` |
| NW watcher | GitHub Pages `/watcher/` |
| 中継スクリプト | Google Apps Script（ウェブアプリとして公開） |
| 記録の保管 | Googleスプレッドシート |

---

## 2. 技術構成

### 2.1 開発言語

いずれも **JavaScript** を中心とし、フレームワークは使用していない。

| 対象 | 言語 | 備考 |
|---|---|---|
| Night Walker / NW watcher | HTML ＋ CSS ＋ JavaScript | 画面・計算・通信のすべて |
| 中継スクリプト（Code.gs） | Google Apps Script | JavaScript を基にした Google の実行環境 |
| サービスワーカー（sw.js） | JavaScript | オフライン起動用 |

**方針**

- **フレームワークを使用しない**（React・Vue 等）。ビルド作業が不要で、ファイルを配置するだけで動作する
- **ES5 相当の記法に統一**（`var` を使用し、アロー関数・class 構文は使わない）。古い端末やブラウザでも確実に動作させるため
- **1ファイル完結**。各アプリはHTML1枚に画面・処理をまとめ、依存関係を最小化している

### 2.2 外部ライブラリ

| ライブラリ | 版 | 用途 | 取得元 |
|---|---|---|---|
| Leaflet | 1.9.4 | 地図の描画 | CDN（unpkg） |

これ以外の外部ライブラリは使用していない。

### 2.3 使用するブラウザ標準機能

| 機能 | 用途 | 非対応時の動作 |
|---|---|---|
| Geolocation API | 位置情報の取得 | ペース計測・天候・位置共有が使えない |
| Fetch API | 天候取得・記録の送受信 | 通信機能が使えない |
| localStorage | 端末内へのデータ保存 | 設定・記録が保持されない |
| Web Share API | 写真・記録ファイルの共有 | 保存とコピーによる代替手段を表示 |
| Web Audio API | ペースチェックの通知音 | 画面表示のみで通知 |
| Screen Wake Lock API | 画面スリープ防止 | 端末設定での対応を案内 |
| Vibration API | ペースチェックの振動通知 | 音のみで通知（iOSは非対応） |
| Service Worker | オフライン起動 | 通信時のみ起動可能 |

いずれも、非対応の場合にアプリ全体が停止しないよう、代替手段または縮退動作を用意している。

### 2.4 外部サービス

| サービス | 用途 | 認証 |
|---|---|---|
| Google Apps Script | 記録の中継・保管 | 不要（ペアコードで識別） |
| Open-Meteo API | 天候・気温・湿度 | 不要 |
| OpenStreetMap | 地図タイル | 不要 |
| Apple Maps / Google Maps | 徒歩ルート案内 | 不要 |
| LINE | メッセージ・写真の送信 | 端末のLINEアプリに委ねる |

いずれも無償の範囲で利用し、APIキーを必要としない構成としている。

### 2.5 開発時に使用した補助ツール

配布物には含まれないが、制作過程で以下を使用した。

| ツール | 用途 |
|---|---|
| Python（Pillow） | アプリアイコン・ルートマップ画像の生成 |
| Python（markdown ＋ wkhtmltopdf） | マニュアル・仕様書のPDF化 |
| Node.js | 算出ロジックの自動テスト（区間集計・ペース判定・端末判定・中継処理など） |

### 2.6 アプリの更新

配布後の更新を、利用者の操作なしに検知できるようにしている。

| 項目 | 内容 |
|---|---|
| 版の管理 | サイト直下の `version.json` と、各アプリの `APP_VERSION` を同じ値に揃える |
| 確認する時 | 起動時と、他のアプリから戻ったとき |
| 取得方法 | `fetch("../version.json?t=<時刻>", {cache:"no-store"})` |
| 異なる場合 | 画面上部に案内を表示（現在の版・新しい版・更新内容） |
| 更新の処理 | Cache Storage を全消去 → サービスワーカーを解除 → 印を付けて再読込 |
| 見送り | 「後で」を選んだ版は記憶し、再度は案内しない |
| 圏外時 | 何も表示せず、通常どおり動作する |

利用者が入力した記録・設定（localStorage）は更新で消えない。
サービスワーカーのキャッシュ名にも版を含めるため、古い版のデータは自動的に破棄される。

### 2.7 実行環境

| 項目 | 内容 |
|---|---|
| 配信 | GitHub Pages（静的ホスティング／HTTPS） |
| 実行 | 各端末のブラウザ（PWAとしてホーム画面から起動） |
| ビルド | 不要。ファイルをそのまま配置する |
| 更新 | `version.json` の版を上げると、利用者に案内が出る |
| サーバー処理 | Google Apps Script のみ |

## 3. 基本要件

### 3.1 機能要件

| ID | 要件 | 実装先 |
|---|---|---|
| F-01 | 全9区間の計画（距離・ペース・時刻）を保持する | 両アプリ |
| F-02 | 到着・出発の実績時刻を記録する | Night Walker |
| F-03 | 実績から到着予想・遅延を算出する | 両アプリ |
| F-04 | 歩行ペースを実測し、計画と比較する | Night Walker |
| F-05 | 進捗を保護者へ共有する | 両アプリ＋中継 |
| F-06 | 現在地の天候を取得・記録する | Night Walker |
| F-07 | LINEで連絡・写真送信を行う | Night Walker |
| F-08 | 地図上でコースと位置を確認する | 両アプリ |
| F-09 | 記録をファイルとして保存する | Night Walker |
| F-10 | 複数の親子を識別して運用する | 全体 |

### 3.2 非機能要件

| 項目 | 要件 |
|---|---|
| 対応端末 | iOS（Safari）／Android（Chrome）。PWAとしてホーム画面に追加して使用 |
| 導入方法 | URLを開き、ホーム画面に追加するだけ。アプリストア不要 |
| 通信 | 記録の送受信・天気・地図で必要。圏外では既存表示のみ継続 |
| 保管 | 端末内 localStorage ＋ スプレッドシート |
| 費用 | すべて無償の範囲で運用 |
| 同時利用 | 複数の親子が同一の中継役を共有可能 |

### 3.3 制約事項（PWAに起因）

| 制約 | 影響 | 対応 |
|---|---|---|
| 画面消灯時に処理が停止 | GPS記録・自動送信が止まる | 到着・出発時は確実に送信。画面スリープ防止機能を用意 |
| バックグラウンド実行不可 | リアルタイム追跡は不可 | 位置は端末標準の「探す」等に委ねる |
| ブラウザからLINE APIを呼べない | 自動送信は不可 | 共有画面で送信先を選ぶ方式 |
| PDF生成が困難 | PDF出力なし | HTML形式で保存し、印刷経由でPDF化可能 |
| localStorage が消える可能性 | 記録の消失 | 中継役にも同時に保存 |

---

## 4. データ仕様

### 4.1 行程データ（設定可能）

行程は定数ではなく、アプリ内の「行程設定」から変更できる。変更内容は端末に保存され、
中継役を経由して見守り側にも自動的に反映される。

**構造**

```
route = {
  title,            // タイトル
  date,             // 開催日（YYYY-MM-DD）
  points: [ {name, lat, lng}, ... ],     // 地点（2か所以上・可変）
  dist:   [ km, ... ],                   // 区間距離（地点数-1個）
  plans:  [ {name, start, legs:[{arrive, rest}, ...]} × 2 ]
}
```

- **到着時刻は絶対時刻**として保持する（"HH:MM"）
- **各区間の出発時刻は導出値**（前の地点の到着＋休憩）
- 日付をまたぐ行程に対応（前の時刻より小さい時刻は翌日と解釈する）
- この構造から、アプリ内部で使う計画配列（距離・速度・歩行時間・日時）を組み立てる

**初期値（2026 Night Walk in 伊勢）**

| 項目 | 内容 |
|---|---|
| 区間数 | 9（地点10か所） |
| 総距離 | 58.4km |
| 出発 | 18:00 |
| 計画1 | 9時着ペース（歩行 12時間20分／休憩 2時間40分） |
| 計画2 | 8時半着ペース（歩行 11時間50分／休憩 2時間40分） |
| 休憩 | 各20分 |

2つの計画は最終区間の所要時間のみが異なる。

### 4.1.1 行程設定の動作

| 操作 | 動作 |
|---|---|
| 地点の追加 | 末尾に追加。区間と各計画の脚も同時に追加する |
| 地点の削除 | その手前の区間も削除する |
| 距離の自動計算 | 2地点の直線距離 × 1.15（道のりの見積り係数） |
| 到着時刻の入力 | ペースを再計算して表示する |
| ペースの入力 | 出発時刻＋（ペース×距離）で到着時刻を計算する |
| 休憩の変更 | 以降の区間の出発時刻が連動する |
| 初期値に戻す | 上記の初期値へ復帰する |

### 4.2 実績データ（localStorage）

**Night Walker**

| キー | 内容 |
|---|---|
| `nightwalk2026_actuals` | 実績時刻 `{"1":{arrive:"19:50",depart:"19:58"}, ...}` |
| `nightwalk2026_weatherlog` | 天候記録 `{"1_arrive":{temp,humidity,code}, ...}` |
| `nightwalk2026_username` | 表示名 |
| `nightwalk2026_paircode` | ペアコード |
| `nightwalk2026_parenttel` | 保護者の電話番号 |
| `nightwalk2026_plan` | 選択中の計画（normal／fast） |
| `nightwalk2026_wakelock` | 画面スリープ防止の設定 |
| `nightwalk2026_route` | 行程データ（行程設定で変更した内容） |

**NW watcher**

| キー | 内容 |
|---|---|
| `nwwatcher_pair` | ペアコード |
| `nwwatcher_tel` | 子の電話番号 |

> 実績時刻は計画をまたいで共通。計画を切り替えても保持される。
> 送信先URLはアプリに埋め込み済みで、利用者からは不可視。

### 4.3 中継役のデータ構造

スプレッドシート1行＝1回の送信。11列。

| 列 | 内容 |
|---|---|
| updatedAt | 受信日時 |
| pair | ペアコード |
| name | 表示名 |
| planKey | 計画種別 |
| event | arrive / depart / ping |
| legId | 地点番号 |
| lat, lng | 緯度・経度 |
| actuals | 実績時刻（JSON） |
| weather | 天候記録（JSON） |
| route | 行程データ（JSON） |

### 4.4 通信仕様

**送信（Night Walker → 中継役）**

```
POST （埋め込みURL）
Content-Type: text/plain;charset=utf-8
Body: {pair, name, planKey, event, legId, lat, lng, actuals, weather, route}
```

> `text/plain` としているのは、`application/json` だとブラウザが事前確認（プリフライト）を行い、
> Apps Script が応答できず失敗するため。

**取得（NW watcher → 中継役）**

```
GET （埋め込みURL）?pair=NW-XXXXXX
→ {ok, empty, updatedAt, pair, name, planKey, event, legId, lat, lng, actuals, weather, route}
```

**リセット**

```
GET （埋め込みURL）?pair=NW-XXXXXX&action=reset
→ 該当ペアの行のみ削除
```

---

## 5. Night Walker（子・歩く人）動作仕様

### 5.1 画面構成

| タブ | 内容 |
|---|---|
| 全体 | 全9区間をカードで一覧 |
| 区間別 | 1区間を拡大表示。前後の区間へ移動可 |
| サマリー | ペースの折れ線グラフ＋記録一覧＋保存 |

固定表示：計画切替、天候表示、親に電話、写真送信、全行程ルート、進捗パネル

### 5.2 区間カード

「区間N ＜起点＞ → ＜終点＞」の単位で構成する。

- 距離／予定ペース／歩行時間
- 起点への「🚶 出発」・終点への「🏁 到着」ボタン
- 予定時刻と実際時刻（実際は編集可・個別クリア可）
- 実績（歩行時間・平均ペース・予定との差・休憩時間）
- 未到着時は到着予想
- ペースチェック

### 5.3 到着・出発の記録

「🏁 到着」「🚶 出発」押下時の処理：

1. 現在時刻（時分）を実績に記録し保存
2. その時点の天候を記録
3. 画面を再描画
4. 中継役へ送信
5. LINEの送信画面を開く（本文は自動生成）

**LINE本文の例（到着時）**

```
19:50に、吉田 茂さんが①ファミマ 大里睦合町店に無事到着しました！

【完了区間】11.0km
所要時間：2時間20分
平均ペース：12分44秒/km
予定との差：8分遅れ
休憩予定：7分

【現地の天候】曇り／21.4℃／湿度78%
```

出発時は、次区間の距離・予定ペース・到着予想を記載する。

### 5.4 到着予想の算出

```
ペース比 = 実際の歩行時間 ÷ 計画の歩行時間   （0.5〜2.0に制限）
補正係数 = これまでの区間のペース比の平均
到着予想 = 直前の出発時刻 ＋ 計画歩行時間 × 補正係数
```

- 実際出発が未入力の場合、「実際到着＋計画休憩時間」を仮の出発時刻とする
- この仮定値は画面の入力欄には反映しない（利用者の入力と区別する）
- 時刻は「時:分」で保持し、深夜0時をまたぐ場合は前後1日から最も近い日付を採用する

### 5.5 ペースチェック

**計測方法**

| 項目 | 値 | 理由 |
|---|---|---|
| 計測窓 | 直近60秒 | 現在のペースを反映するため |
| 最低計測時間 | 40秒 | 短時間では誤差が大きいため |
| 最低移動距離 | 25m | GPSの揺らぎと区別するため |
| 採用する測位精度 | 30m以内 | 精度不良が距離を歪めるため |
| 距離の求め方 | 始点と終点の直線距離 | 連続点の累積はGPSの揺らぎを足し込むため |

> 天候取得・見守り送信には、精度を問わず最新の位置を使う（屋内でも動作させるため）。

**動作**

1. ボタン押下で計測開始。以後1秒ごとに判定
2. 条件を満たすまで「計測中です（あと約N秒）」を表示
3. 計測できた時点で、結果表示・音・振動で通知

**判定**

| 判定 | 条件 | 色 | 音 |
|---|---|---|---|
| 速すぎます | 予定より1分/km以上速い | 黄 | 高音3回 |
| 順調です | ±1分/km未満 | 青 | 上昇する2音 |
| 遅れています | 予定より1分/km以上遅い | 赤 | 低い2音 |

> 音は Web Audio API で生成。iOSの制約により、ボタン押下時に無音を再生して発音可能な状態を作る。

### 5.6 天候

- Open-Meteo API から気温・湿度・天候コードを取得
- 起動時と10分ごとに更新
- WMOの天候コードを日本語に変換（快晴／曇り／小雨／雷雨 等）
- 到着・出発の記録時に、その時点の値を保存

### 5.7 写真の送信

iOSの制約により、共有は「利用者のタップ直後」でなければ実行できない。撮影直後は操作とみなされないため、次の流れとする。

1. ボタン押下でカメラ起動
2. 撮影後、写真・説明文・送信ボタンを画面に表示
3. **「📤 LINEなどに送る」押下時に共有を実行**（確実に操作直後となる）
4. 共有不可の場合に備え「写真を保存」「文面をコピー」を常時表示

説明文は「時刻＋名前＋現在地点」を自動生成する。

### 5.8 記録の保存

サマリータブから、グラフと記録一覧を含むHTMLファイルとして保存する。

- 共有機能があれば保存先を選択、なければダウンロード
- 保存したファイルはブラウザで開け、印刷経由でPDF化も可能

### 5.9 到着・出発時のメッセージ

ボタン押下時に、状況に応じた文面を画面に表示する。

**状況の判定材料**

| 項目 | 算出 |
|---|---|
| 経過時間 | 出発からの経過分 |
| 予定との差 | 実際の時刻 − 計画の時刻 |
| ペースの差 | 実際の平均ペース − 計画ペース（到着時のみ） |

**文面の選び方**

| 場面 | 条件 | 調子 |
|---|---|---|
| 到着 | ペースが1分/km以上速い | 称賛 |
| 到着 | 予定より5分以上早い | 称賛 |
| 到着 | ペースが1.5分/km以上遅い／20分以上の遅れ | ねぎらい・休息の促し |
| 到着 | それ以外 | 祝福 |
| 出発 | 5分以上早い | 快調を伝える |
| 出発 | 20分以上遅い | 焦らせない励まし |
| 出発 | それ以外 | 通常の励まし |

同じ場面でも複数の候補から選ぶため、毎回同じ文面にはならない。

**スタート・ゴールの演出**

出発地点の出発時と、最終地点の到着時は全画面の演出とする。

| 要素 | 内容 |
|---|---|
| 背景 | 夜空のグラデーション＋またたく星（40個） |
| 図 | スタート＝歩く人と月／ゴール＝トロフィー |
| 動き | 拡大しながらの出現、上下の揺れ、光の輪 |
| 紙吹雪 | ゴールのみ70個が降る |
| 音 | Web Audio によるファンファーレ（ゴールは長く華やか） |
| 振動 | 対応端末のみ |
| 表示 | 出発＝全行程と予定／ゴール＝所要時間・到着・予定との差・距離 |

ゴールの演出を閉じると、記録画面へ移る。

### 5.10 画面スリープ防止

Screen Wake Lock API により、画面を消さない状態を維持できる。ペース計測時の使用を想定。非対応端末では、端末設定での対応を案内する。

---

## 6. NW watcher（親・見守る人）動作仕様

### 6.1 画面構成

| 領域 | 内容 |
|---|---|
| 最終更新 | 「◯◯さん・最終更新 12分前」 |
| 現在の状況 | 到着済み地点・休憩中・次の目的地 |
| ゴール到着予想／予定との差 | 遅れは赤、前倒しは緑 |
| 進捗バー | 通過地点数と残り距離 |
| 操作ボタン | 地図／探す／電話／更新 |
| 通過記録 | 全10地点の予定・実際・遅延 |

### 6.2 更新

- 60秒ごとに自動取得
- 30秒ごとに経過時間表示のみ更新
- 手動更新ボタンあり

> **更新遅延の警告表示は行わない。**
> PWAの仕様上、子の画面消灯中は送信が止まり、休憩地点の間隔（1〜2時間）で更新が空くのが正常なため。

### 6.3 算出処理

Night Walker と**同一のロジック**を用いる。受信した実績データから、親側で独立に到着予想・遅延を算出する。

> 両アプリの算出結果が一致することを、8パターン（実績なし／到着のみ／深夜0時またぎ／大幅遅延／計画変更／完走 等）で検証済み。

### 6.4 地図

Leaflet ＋ OpenStreetMap で描画。

- コース全10地点を S・①〜⑧・G のピンで表示
- 最後に受信した子の位置を青丸で表示
- ピン押下で地点名を表示

### 6.5 記録のリセット

中継役へリセット要求を送り、**該当ペアの記録のみ**を削除する。他の親子の記録および子の端末内の記録は保持される。

---

## 7. ペアリング仕様

### 7.1 目的

1つの中継役を複数の親子が共有しつつ、記録を混在させない。

### 7.2 ペアコード

| 項目 | 内容 |
|---|---|
| 形式 | `NW-` ＋ 英数6文字（例：`NW-8F3K2Q`） |
| 文字種 | 大文字英字と数字から、紛らわしい文字（0・O・1・I）を除いた32種 |
| 組合せ数 | 32⁶ ≒ 約10.7億 |
| 生成 | Night Walker が自動生成 |
| 受け渡し | LINEで保護者へ送信 |
| 検証 | 中継役で6文字以上を必須とする |

### 7.3 運用パターン

| 構成 | 方法 |
|---|---|
| 子1・親1 | 同じペアコードを設定 |
| 子1・親2 | 両親が**同じ**ペアコードを設定。互いに干渉しない |
| 子2・親1 | 見守り側に**2つのペアコードを登録**し、画面上部のタブで切り替える（最大5人） |
| 子2・親1（同行） | 同一コードでも可。ただし表示は最後に送信した側になる |

### 7.4 複数の子の見守り

NW watcher は複数の子を登録でき、タブで切り替えて確認できる。

| 項目 | 内容 |
|---|---|
| 保持形式 | `[{pair, name, tel}, ...]`（最大5件） |
| 取得 | 更新のたびに**全員ぶんを並行して取得**し、端末内に保持する |
| 切替 | 取得済みのため即座に表示が切り替わる |
| 名前 | 子が登録した名前を受信時に自動で設定する |
| タブ表示 | 名前と、それぞれの最終更新（「12分前」など） |
| 電話 | 表示中の子の番号にかける |
| リセット | 表示中の子の記録のみ削除する |
| 1人の場合 | タブを表示しない（従来と同じ見た目） |
| 旧設定 | 単一ペアコードの設定は、起動時に自動で引き継ぐ |

一部の子が取得できない場合でも、他の子の表示には影響しない。

---

## 8. 端末による動作の差異

判定はユーザーエージェントで行い、自動的に切り替える。操作方法は変わらない。

| 機能 | iOS | Android |
|---|---|---|
| ホーム画面追加 | 共有ボタン →「ホーム画面に追加」 | メニュー →「アプリをインストール」 |
| 地点へのルート | Apple Maps | Googleマップ |
| 全行程ルート | Apple Maps（`+to:` 連結） | Googleマップ（waypoints） |
| 天候表示のタップ | 天気アプリ | 天気の検索結果 |
| 位置の確認（親） | 「探す」アプリ | Googleデバイスを探す |
| LINE共有 | `line://msg/text/` | `https://line.me/R/share` |
| 振動 | 非対応 | 対応 |

> **子がiOS・親がAndroidの場合、「探す」では子を探せない。**
> iCloud（web）またはGoogleマップの現在地共有を、事前に設定する必要がある。

---

## 9. セキュリティ

| 項目 | 方針 |
|---|---|
| 送信先URL | アプリに埋め込み。公開リポジトリ上のため閲覧は可能 |
| 記録の保護 | ペアコードを知る者のみ閲覧可（約10.7億通り） |
| 想定リスク | URLを知った第三者による無関係データの書き込み |
| 個人情報 | 氏名・電話番号は端末内のみ。氏名は中継役にも保存される |
| マニュアル | 推測困難なファイル名・検索避け・リンク非掲載 |
| 事後対応 | Apps Script を再デプロイすれば旧URLを無効化できる |

> 中継役には、利用する全ての親子の記録（氏名・位置・通過時刻）が集約される。
> 他家庭へ配布する場合は、その旨を事前に周知することが望ましい。

---

## 10. 検証状況

### 10.1 自動検証済みの項目

| 対象 | 内容 |
|---|---|
| 算出ロジック | 両アプリの結果一致（8パターン） |
| 実績の扱い | 未入力時に自動補完されない／内部計算は継続 |
| ペース計測 | 静止時のGPS揺らぎを移動と誤認しない／計測時間不足を検知 |
| 判定方向 | 予定12分・実際10分 → 「速い」 |
| LINE本文 | 距離・所要時間・平均ペース・遅延・天候の算出 |
| 中継役 | 複数ペアの分離／リセットの範囲／不正コードの拒否 |
| 端末判定 | iOS・Android・iPadの判別と、リンクの出し分け |
| URL埋め込み | 端末に古い値が残っていても埋め込み値を優先 |

### 10.2 実機確認が必要な項目

| 項目 | 状況 |
|---|---|
| iOS 一連の動作 | 確認済み |
| Android 一連の動作 | **未確認** |
| ペースチェックの実地計測 | 屋外での確認を推奨 |
| 写真共有（Android） | **未確認** |

---

## 11. 既知の制約

| 項目 | 内容 |
|---|---|
| バックグラウンド動作 | 画面消灯中は記録・送信が停止する |
| リアルタイム追跡 | 不可。端末標準の位置共有を併用する |
| LINE自動送信 | 不可。毎回、送信先を選択する |
| 電池 | GPS利用のため消費が大きい。モバイルバッテリーを推奨 |
| データ保持 | iOSがPWAのデータを削除する場合がある（中継役に控えあり） |
| オフライン | アプリ本体は起動可。地図・天気・送信は不可 |

---

## 12. 変更履歴

| 版 | 日付 | 内容 |
|---|---|---|
| 1.0 | 2026-07-30 | PWA版として初版作成 |
| 1.1 | 2026-07-30 | 「2. 技術構成」を追加（開発言語・ライブラリ・使用API・実行環境） |
| 1.2 | 2026-07-30 | 表記修正（「工程」→「行程」）。運用パターンをマニュアルへ反映 |
| 2.1 | 2026-07-31 | 見守り側で複数の子を切り替えられるようにした。行程設定の画面レイアウトを修正 |
| 2.0 | 2026-07-31 | 行程を設定可能に変更（行程設定の追加・行程データの親子同期）。到着・出発時のメッセージと、スタート／ゴールの演出を追加。行程を新コース（58.4km・18:00出発）へ更新。アプリ内更新の仕組みを追加 |
